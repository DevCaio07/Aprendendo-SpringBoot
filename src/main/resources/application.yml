spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fridge
    username: root
    password: Vida2109.

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true